<controls:BaseWindow x:Class="NumTag.Views.MainWindow"
        xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:NumTag.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:NumTag.Controls"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/logo.ico" Title="NumTag" Closing="Window_OnClosing"
        ShowInTaskbar="False" Topmost="True" WindowState="FullScreen"
        ShowActivated="True" WindowVisible="{Binding WindowVisible}">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <Grid Background="{Binding Behavior.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" SharedSizeGroup="Hint"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" SharedSizeGroup="Hint"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Margin="20">
            <DockPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Margin="20" Text="{Binding Behavior.Subtitle}" FontSize="{Binding Behavior.SubtitleTextSize}" Foreground="{Binding Behavior.Foreground}" DockPanel.Dock="Bottom" TextAlignment="Center"/>
                <TextBlock Text="{Binding Behavior.Title}" FontWeight="Bold" FontSize="{Binding Behavior.TitleTextSize}" Foreground="{Binding Behavior.Foreground}" TextAlignment="Center"/>
            </DockPanel>
        </Grid>
        <TextBlock Grid.Row="2" Margin="10" Text="{Binding Behavior.Hint}" FontSize="{Binding Behavior.HintTextSize}" Foreground="{Binding Behavior.HintForeground}" HorizontalAlignment="Center"/>
    </Grid>

</controls:BaseWindow>
